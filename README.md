Once you download the file,used winrar to upzip the file. 
Then open intellij and open unzip file. I
t will takes time to setup everything in your system. 
Then go to HelloApplication and run the code
